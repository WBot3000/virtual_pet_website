import React from 'react';

function Logout() {
    const onSuccess = (res) => {
        alert('Logout successful');
    };

    return (
        <div>
            <h2>TODO</h2>
        </div>
    );
}

export default Logout;